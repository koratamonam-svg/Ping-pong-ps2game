name: Build PS2 Game
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container: ps2dev/ps2sdk:latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          # نستخدم المسار الكامل للمترجم والمكتبات
          make
          
